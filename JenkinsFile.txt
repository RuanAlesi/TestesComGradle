pipeline {
    agent any
    stages {
        stage('Build'){
            steps {
                sh './gradlew clean build'
                script {
                    sh 'gradle test'
                }
            }
        }
    }
    post {
        always {
            junit 'build/reports/**/*.xml'
        }
    }
}
